Rect Utility
============

Usage
-----

~~~ js
import {getOffset} from '@taufik-nurrohman/rect';

console.log(getOffset(document.querySelector('#pane')));
~~~

Methods
-------

### getAxis(event, node)

### getOffset(node)

### getRect(node)

### getSize(node)

### getScroll(node)

### setOffset(node, data)

### setSize(node, data)

### setScroll(node, data)